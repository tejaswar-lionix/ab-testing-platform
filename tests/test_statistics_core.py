"""Tests for statistics core distinct"""

def test_statistics_core_0():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha

def test_statistics_core_1():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha

def test_statistics_core_2():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha

def test_statistics_core_3():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha
