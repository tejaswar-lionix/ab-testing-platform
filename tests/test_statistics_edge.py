"""Tests for statistics edge distinct"""

def test_statistics_edge_0():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha

def test_statistics_edge_1():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha

def test_statistics_edge_2():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha

def test_statistics_edge_3():
    p=0.03
    alpha=0.05/2
    assert p < alpha or p >= alpha
